프로젝트명 - 미정
*** 이미지 파일은 : \WEB-INF\IMAGES    폴더에
*** 자바스크립트 파일 / CSS 파일은 파일로 분리 작업 지향 (간단한 것은 인라인(in-line)으로 처리)
*** sql 패키지에 들어갈 xml 파일명 - 테이블이름SQL.xml	// boardSQL.xml

*** 데이터명 : DTO(x) -> VO사용			//BoardDTO(x) -> BoardVO
*** 패키지명 : 소문자 사용				//gw.board
*** 클래스명 : 첫(!)글자 대문자 사용			//BoardVO
*** 액션명 : 작업명Controller			//BoardController
*** 컨트롤러xml : 작업명-servlet		//board-servelt.xml

*** 테이블 생성 쿼리문 보관 (CRAETE 문을 모아놓는 TXT파일)
ex) [tableCreate.txt]
CREATE TABLE MEMBER(
	ABCD VARCHAR2(100),
	EFGH VARCHAR2(100),
	REG_DATE SYSDATE
);

*** 시퀀스명 - 적용할 컬럼이름_SEQ
ex)
create sequence board_code_seq nocache